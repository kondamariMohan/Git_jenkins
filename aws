#!/bin/bash

# This is a comment
echo "Hello, Bash Scripting!"

name="User"
echo "Welcome, $name!"

# Conditional statement
if [ -d "/tmp/my_directory" ]; then
    echo "Directory /tmp/my_directory already exists."
else
    mkdir /tmp/my_directory
    echo "Created directory /tmp/my_directory."
fi
